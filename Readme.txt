$ docker-compose up -d --build

- d หรือ daemon คือการสั่งรัน docker container ใน mode background นะครับ
-- build คือคำสั่งสำหรับการ pull image เพื่อที่จะสร้าง image สำหรับการนำมาสร้าง container ส่วนใหญ่จะใช้ เฉพาะเวลาเราต้อง build image ใหม่นะครับ